#include <iostream>
using namespace std;
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n, x, a[2 * 100000];
        cin >> n >> x;
        for (size_t i = 0; i < n; i++)
        {
            cin >> a[i];
        }
    }
    return 0;
}